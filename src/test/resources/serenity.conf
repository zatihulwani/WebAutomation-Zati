webdriver {
driver = chrome
autodownload = true
}

headless.node = false #option true will show the browser running

serenity {
    take.screenshots = FOR_FAILURES
}

#
# Chrome options can be defines using chrome.switches property
#
chrome.switches = """--start-maximized;"""